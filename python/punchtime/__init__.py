"""Initialize the punchtime package.
The most notable side-effect of importing this is package is that it adds a
NOTIFY (25) log level and notify() method to the global logger object.
"""

__version__ = "0.0.1"
